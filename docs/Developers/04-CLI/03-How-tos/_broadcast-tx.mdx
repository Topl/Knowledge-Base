To broadcast a simple transaction run the following command:

<pre><code>
brambl-cli tx broadcast -i {props.txFileProved} -h $HOST --port $PORT 
</code></pre>

<p>This will broadcast the transaction in the file <code>{props.txFileProved}</code> to the network.</p>